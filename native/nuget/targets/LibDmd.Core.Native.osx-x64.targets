<?xml version="1.0" encoding="utf-8"?>
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <!-- Copy the osx-x64 native binaries next to the consumer's output (best-effort for non-RID
       builds; RID-specific publish and Unity resolve them on their own). -->
  <!-- Select by the requested RID's architecture when a RID is set (e.g. `publish -r osx-arm64` must
       NOT pull x64 dylibs even on an Intel host); fall back to the build host's ProcessArchitecture
       only for non-RID builds. -->
  <ItemGroup Condition="$([MSBuild]::IsOSPlatform('OSX')) And ( ( $(RuntimeIdentifier.StartsWith('osx')) And $(RuntimeIdentifier.EndsWith('x64')) ) Or ( '$(RuntimeIdentifier)' == '' And '$([System.Runtime.InteropServices.RuntimeInformation]::ProcessArchitecture)' == 'X64' ) )">
    <None Include="$(MSBuildThisFileDirectory)..\..\runtimes\osx-x64\native\*">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <Link>%(Filename)%(Extension)</Link>
      <Visible>false</Visible>
    </None>
  </ItemGroup>
</Project>